## 前端修复计划

### 1. 修复 app.js - 添加快速操作按钮事件处理

修改 `setupEventListeners()` 函数，添加 `.action-btn` 的点击事件处理：

```javascript
function setupEventListeners(){
  // 导航标签
  $$(".nav-tab").forEach(t=>{t.onclick=()=>State.setTab(t.dataset.tab);});
  // 快速操作按钮
  $$(".action-btn").forEach(btn=>{btn.onclick=()=>State.setTab(btn.dataset.tab);});
  // 其他事件绑定...
}
```

### 2. 修复 pages.css - 添加导航按钮指针样式

```css
.nav-tab, .action-btn {
  cursor: pointer;
}
```

### 3. 修复测试验证

运行程序验证：
- 菜单栏Tab切换正常
- 快速操作按钮正常跳转
- 主题切换按钮正常响应
- 所有按钮点击事件正常